<h1 style="text-align: center;">New Movie</h1>

<%= simple_form_for [@list, @bookmark], html: { style:"width: 50%; margin: 0 auto;" } do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Submit review", class: "btn btn-lg bg-dark text-light" %>
<% end %>
